<form method="post" action="{{ path('app_voypersonalise_delete', {'id': voypersonalise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voypersonalise.id) }}">
    <button class="btn">Delete</button>
</form>
